---
extends: default

yaml-files:
  - "*.yaml"
  - "*.yml"
  - ".yamllint"

rules:
  # 140 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 140
    level: warning
  comments:
    min-spaces-from-content: 1 # prettier compatibility
